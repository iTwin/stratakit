<style is:global>
	@layer page {
		status-badge {
			display: inline-block;
			font-weight: 600;
			padding-inline: 8px;
			margin-block: -2px;
			border-radius: var(--stratakit-radius-round);
			border: 1px solid var(--stratakit-color-border-neutral-base);
		}
	}
</style>
