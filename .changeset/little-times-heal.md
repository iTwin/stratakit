---
"@stratakit/mui": patch
---

Updated the default portal container of `Autocomplete`, `Dialog`, `Drawer`, `Menu`, `Modal`, `Popover`, `Popper`, `Select`, `SwipeableDrawer` and `Tooltip` components to use the root portal container instead of the `<body>` element.
