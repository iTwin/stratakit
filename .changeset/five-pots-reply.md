---
"@stratakit/mui": patch
---

Updated `Chip` component so the `root` element is no longer interactive. Deletable `Chip` now renders an interactive clear button, and clickable `Chip` renders its label as an interactive button.
