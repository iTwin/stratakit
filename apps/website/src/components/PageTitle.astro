---
// See https://starlight.astro.build/reference/overrides/#pagetitle

import { getEntry } from "astro:content";
import Breadcrumb from "./Breadcrumb.astro";

const { entry } = Astro.locals.starlightRoute;

/**
 * Generates breadcrumbs for API reference pages based on route id
 * Returns breadcrumbs for API pages, undefined for all other pages.
 */
async function getBreadcrumbsFromRoute(id: string | undefined) {
	if (!id) {
		return undefined;
	}

	// Only return breadcrubms for the API reference pages, and we need at least 3 parts: reference/packageId/apiName
	const parts = id.split("/");
	if (parts.length < 3 || parts[0] !== "reference") {
		return undefined;
	}

	const packageId = parts[1];
	const apiName = parts.slice(2).join("/"); // In case there are more parts
	const baseUrl = import.meta.env.BASE_URL;

	const pkg = await getEntry("packages", packageId);
	const packageLabel = pkg?.data.name ?? `@stratakit/${packageId}`;

	return [
		{ label: "API Reference", href: `${baseUrl}/reference/` },
		{
			label: packageLabel,
			href: `${baseUrl}/reference/#${packageId}`,
		},
		{ label: apiName },
	];
}

const status = entry.data.status;
const links = entry.data.links || {};
const breadcrumbs = await getBreadcrumbsFromRoute(entry.id);

const muiDocsUrl = links.muiDocs;
const componentStatusOverviewUrl = `${import.meta.env.BASE_URL}/components/overview/#component-status`;
const apiReferenceUrl = (() => {
	const url = links.apiReference;
	if (!url) return undefined;

	if (url.startsWith("/")) {
		return `${import.meta.env.BASE_URL}${url}`;
	}

	return url;
})();
---

{breadcrumbs && <Breadcrumb crumbs={breadcrumbs} />}

<hgroup>
	<h1 id="_top">{entry.data.title}</h1>
	{entry.data.description && <p>{entry.data.description}</p>}
</hgroup>

<dl>
	{
		status && (
			<div class="inline-item">
				<dt>
					<a href={componentStatusOverviewUrl}>Component status</a>:
				</dt>
				<dd>
					<status-badge>{status}</status-badge>
				</dd>
			</div>
		)
	}
	{
		Object.keys(links).length > 0 && (
			<div class="external-links">
				<dt>Useful links:</dt>
				<dd>
					<ul role="list">
						{muiDocsUrl && (
							<li>
								<a href={muiDocsUrl}>MUI docs</a>
							</li>
						)}
						{apiReferenceUrl && (
							<li>
								<a href={apiReferenceUrl}>API reference</a>
							</li>
						)}
					</ul>
				</dd>
			</div>
		)
	}
</dl>

<style>
	@layer overrides {
		h1 {
			font-size: var(--sl-text-h1);
			line-height: var(--sl-line-height-headings);
			font-weight: 600;
			color: var(--sl-color-white);
			text-box: cap alphabetic;
		}

		h1 + p {
			margin-top: var(--stratakit-space-400);
			color: var(--stratakit-color-text-neutral-secondary);
		}

		ul {
			list-style-type: none;
			display: grid;
			gap: var(--stratakit-space-100);
		}

		a:where([href^="https://"])::after {
			content: "↗";
			content: "↗" / "External";
			position: absolute;
			margin-inline-start: 2px;
		}

		.inline-item {
			display: flex;
			gap: var(--stratakit-space-100);

			dt,
			dd {
				margin: 0;
			}
		}

		dl {
			&:not(:has(> *)) {
				display: none;
			}
		}

		dt {
			margin-block-end: var(--stratakit-space-100);
		}

		dt a {
			font-weight: 600;
		}

		dd a {
			font-weight: 400;
		}
	}
</style>
