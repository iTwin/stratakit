---
"@stratakit/icons": patch
---

Added `#filled` and `#filled-large` variations of the following icons:

- `filter.svg` (`svgFilterFilled` and `svgFilterFilledLarge`)
- `star.svg` (`svgStarFilled` and `svgStarFilledLarge`)
