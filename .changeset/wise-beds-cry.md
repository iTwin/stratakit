---
"@stratakit/structures": minor
---

Renamed `DropdownMenu.Root` component to `DropdownMenu.Provider` to maintain consistency with the naming convention, where provider components have no underlying DOM element.
