---
"@stratakit/structures": patch
---

Added `ReactNode[]` type support for `items` prop of `ErrorRegion.Root` component which is used to determine error region visibility. Previously supported `ReactNode` type is now deprecated and will be removed in a future release.
